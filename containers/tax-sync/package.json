{
  "name": "@repo/tax-sync",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "dev:server": "bun --watch src/server.ts",
    "start:server": "bun src/server.ts",
    "docker:build": "docker build -t repo-tax-sync -f Dockerfile ../.."
  },
  "dependencies": {
    "@repo/database": "workspace:*",
    "cheerio": "^1.0.0",
    "drizzle-orm": "^0.41.0",
    "got-scraping": "^4.1.3",
    "tough-cookie": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5.8.2"
  }
}
